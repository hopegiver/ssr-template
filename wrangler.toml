name = "ssr-template"
compatibility_date = "2024-12-26"

# Pages 프로젝트 설정
pages_build_output_dir = "."

# 로컬 개발 서버 설정
[dev]
port = 8788
local_protocol = "http"

# 환경 변수 (필요시 추가)
[vars]
# ENVIRONMENT = "development"
