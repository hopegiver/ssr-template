{
  "name": "ssr-template",
  "version": "1.0.0",
  "description": "Cloudflare Pages SSR Template",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages publish ."
  },
  "dependencies": {
    "liquidjs": "^10.19.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}